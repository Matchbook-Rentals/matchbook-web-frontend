# MatchBook Worker Service Configuration

# Server
server.port=${PORT:8080}
spring.application.name=matchbook-worker

# Redis Configuration
# Railway uses REDISHOST, REDISPORT, REDIS_PASSWORD (mixed naming)
redis.host=${REDISHOST:${REDIS_HOST:localhost}}
redis.port=${REDISPORT:${REDIS_PORT:6379}}
redis.password=${REDIS_PASSWORD:}
redis.use-ssl=${REDIS_USE_SSL:false}

# Resend API
resend.api-key=${RESEND_API_KEY}

# Email Queue
email.queue.enabled=${EMAIL_QUEUE_ENABLED:true}

# Logging
logging.level.root=INFO
logging.level.com.matchbook.worker=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} - %logger{36} - %msg%n

# Actuator (monitoring)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

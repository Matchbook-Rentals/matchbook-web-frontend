/**
 * Search and Application Constants
 *
 * These constants define limits and constraints for housing search and application functionality.
 */

/**
 * Maximum number of open applications allowed per trip
 * An "open" application is one that hasn't been matched yet
 */
export const MAX_APPLICATIONS_PER_TRIP = 5;

/**
 * Maximum number of total open applications allowed across all trips
 * An "open" application is one that hasn't been matched yet
 */
export const MAX_APPLICATIONS_TOTAL = 10;

# Test environment configuration
# Copy this file to .env.test and fill in your test credentials
#
# IMPORTANT: Create these accounts ONCE in your Clerk dev instance, then reuse them.
# This prevents spamming Clerk with new test users on every test run.

TEST_USER_EMAIL=your.test.email@example.com
TEST_USER_PASSWORD=your_test_password
TEST_BASE_URL=http://localhost:3000

# Admin user for article management tests (must have admin/staff role)
TEST_ADMIN_EMAIL=your.admin.email@example.com
TEST_ADMIN_PASSWORD=your_admin_password

# ============================================================================
# BOOKING FLOW TEST ACCOUNTS (Required for booking-flow.spec.ts)
# ============================================================================
# Create these accounts manually in your Clerk dev dashboard:
# 1. Go to Clerk Dashboard > Users > Create User
# 2. Create a "host" account (for creating listings, approving applications)
# 3. Create a "renter" account (for searching, applying, signing leases)
# 4. Fill in the credentials below
#
# If these are not set, tests will create new accounts each run (not recommended)
# ============================================================================

# Host account - for creating listings and approving applications
TEST_HOST_EMAIL=testhost@yourdomain.com
TEST_HOST_PASSWORD=your_host_password

# Renter account - for creating trips, applying, and booking
TEST_RENTER_EMAIL=testrenter@yourdomain.com
TEST_RENTER_PASSWORD=your_renter_password